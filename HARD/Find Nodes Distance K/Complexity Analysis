Time complexity:
O(n)
As we approach the problem using parent, the input behave like graph.
O(V+E) for BFS in the graph
But for tree E is always smaller than n . Hence O(V+E) < 2n --> O(n)

Space complexity:
O(n)