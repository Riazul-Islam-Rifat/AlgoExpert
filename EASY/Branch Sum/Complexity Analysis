Time complexity:
O(n) where n is the number of nodes and all the nodes are traversed.

Space complexity:
O(n) as max leaf nodes could be roughly n/2.
* Number of branch sum never exceeds number of nodes.