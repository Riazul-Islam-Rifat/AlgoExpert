Time and space complexity both O(1) as we know the max length of the given string will be 12.
Hence the program doesn't depends on the input size. And at max there will 2^32 addresses.
The number seems large still we know exactly the upper bound so we can consider the complexity as O(1)