# Time complexity:
 O(n * n!) # For n! permutations n recursive calls and inside the for loop O(1) times operations.
# Space complexity:
O(n * n!) # Total n! permutations and each of them is length of n